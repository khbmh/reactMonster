import React from 'react';

const JsxRules= () => {
  return (
    <div>
      <h1>Jsx Rules</h1>
      <p>
        <li>inside one component inside one parent all codes to be wrapped!</li>
        <li>all elements must have closing tag, all tag must be closed</li>
        <li>attributes are to be written in camel case : className</li>
      </p>
    </div>
  );
};

export default JsxRules
